apiVersion: 1

datasources:
  - name: TimescaleDB
    uid: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    database: keenetic_monitor
    user: keenetic
    secureJsonData:
      password: keenetic
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
